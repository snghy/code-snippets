=LET(addr_pflen,TEXTSPLIT(A1,"/"),
octs,INT(TEXTSPLIT(INDEX(addr_pflen,1),".")),
ip,SUM(octs*{167772160,65536,256,1}),
bwidth,32-INDEX(addr_pflen,2),
width,BITLSHIFT(1,bwidth),
assert,REDUCE(TRUE,MAP(octs,LAMBDA(n,AND(n>=0,n<=255))),AND),
assert2,BITAND(BITLSHIFT(1,bwidth)-1,ip)=0,
ok,AND(assert,assert2),
HSTACK(ip,ip+width-1,ok))
